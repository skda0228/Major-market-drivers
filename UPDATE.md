# 시장 레퍼런스 페이지 — 배포 & 매일 갱신 지시서

이 문서는 두 종류의 독자를 위한 것입니다.

- **Claude Code (최초 1회)** — 이 페이지를 `skda0228/Major-market-drivers` 리포에 올려 GitHub Pages로 서비스하는 작업. → §1
- **모바일 Claude (매일)** — "오늘자로 업데이트해줘" 한마디로 수행할 갱신 규칙. → §2, §3

---

## 1. 최초 배포 (Claude Code에게)

### 옮길 파일

| 이 프로젝트 | 리포 (`skda0228/Major-market-drivers`) |
| --- | --- |
| `templates/market-calendar/index.html` | `index.html` (기존 파일 교체) |
| `templates/market-calendar/indicators.json` | `indicators.json` |
| `templates/market-calendar/UPDATE.md` | `UPDATE.md` |
| 이 디자인 시스템의 `styles.css` | `styles.css` |

### 반드시 지킬 것

1. **`index.html` 안의 `<script src="./ds-base.js">` 줄을 지우고** 대신 `<head>`에 다음 한 줄을 넣습니다. 리포에는 디자인 시스템 번들이 없으므로 스타일시트만 직접 링크합니다.
   ```html
   <link rel="stylesheet" href="styles.css">
   ```
2. **첫 줄의 `<!-- @template ... -->` 주석은 삭제**합니다 (이 프로젝트 전용 표식).
3. `index.html`은 `indicators.json`을 **같은 디렉터리에서 fetch** 합니다. 두 파일은 항상 같은 폴더에 둡니다. 로컬에서 `file://`로 열면 fetch가 차단되니, 확인은 `python3 -m http.server` 같은 로컬 서버나 GitHub Pages에서 합니다.
4. GitHub Pages 설정: Settings → Pages → Source `main` / `/ (root)`.
5. 폰트는 CDN(Google Fonts, IBM Plex Sans KR)에서 불러옵니다. 오프라인 배포가 필요하면 폰트를 리포에 벤더링하고 `@import`를 교체합니다.

### 배포 확인
- 10개 지표 카드가 그려지는가 (안 그려지면 `indicators.json` 경로/JSON 문법 문제)
- 카드마다 그래프가 2개(1Y·1M) 나오고, 각 그래프 위에 기간 라벨이 붙는가
- 카드가 한 줄에 5개씩 놓이는가(화면이 좁으면 3 → 2 → 1개로 줄어듦)
- 빨강 액센트 + IBM Plex Sans KR로 렌더되는가 (안 되면 `styles.css` 링크 실패)

---

## 2. 매일 갱신 — 무엇을 고치는가

**대부분의 날은 `indicators.json` 하나만 고치면 끝납니다.**

### 2-1. `indicators.json` 구조

```jsonc
{
 "asOf": "2026-07-31",      // 마지막 거래일. 두 그래프의 오른쪽 끝.
 "yearMonths": 12,          // 왼쪽 1Y 그래프가 덮는 기간(개월)
 "recentMonths": 1,         // 오른쪽 1M 그래프가 덮는 기간(개월)
 "items": [
  {
   "name": "코스피",          // 카드 제목
   "code": "KOSPI",          // 우상단 코드
   "dir": "up",              // up = 빨강 / dn = 파랑 / nt = 회색(방향이 호재·악재가 아닌 지표) — 한국식 등락 색상
   "value": "6,595.45",      // 종가 (문자열, 천단위 콤마 포함)
   "change": "▲ 1,001.89 (+17.91%)",  // 전일 대비. 오름 ▲ / 내림 ▼
   "note": "...",            // 카드 하단 한 줄 설명. <b> 태그 사용 가능
   "y": [ ... ],             // 1Y 그래프용 — 월간 종가 12개
   "q": [ ... ]              // 1M 그래프용 — 일간 종가 21~23개
  }
 ]
}
```

**`y` / `q` 두 배열 규칙**
- `y` = **월간 종가**, 12개(1년). 마지막 값 = `asOf` 당월 종가.
- `q` = **일간 종가**, 21~23개(최근 1개월, 거래일만). 마지막 값 = `asOf` 당일 종가.
- 두 배열의 **마지막 값과 `value`는 반드시 일치**시킵니다. 어긋나면 그래프 끝점이 헤드라인 숫자와 다른 곳을 가리킵니다.
- 각 그래프는 **자기 배열의 최고·최저에 맞춰 세로 축이 자동 조정**됩니다. 두 그래프의 높낮이를 서로 비교하면 안 되고, 페이지에도 그렇게 안내돼 있습니다.
- 눈금 날짜는 `asOf`와 기간으로 자동 계산되므로 **날짜는 따로 적지 않습니다.**

**매일 갱신할 때**
- `q`: **하루가 끝날 때마다 맨 앞을 하나 빼고 맨 뒤에 오늘 종가를 추가**(길이 21~23 유지). 장중에 고칠 때는 맨 뒤 값만 교체하고 개수는 그대로 둡니다. 휴장일은 아무것도 추가하지 않습니다.
- `y`: 달이 바뀌었을 때만 맨 앞을 빼고 맨 뒤에 지난달 종가를 추가. 월중이면 **맨 뒤 값만 교체**.
- `value`·`change`는 매번 갱신.

**카드 순서 = 배열 순서.** 현재 10종(코스피·S&P500·나스닥·다우존스·니케이225·미10년물·공포지수·원/달러·WTI·금)이라 5열 × 2줄로 떨어집니다. 개수를 바꾸면 정렬이 흐트러지니 **10의 배수 또는 5의 배수**를 유지하는 편이 좋습니다.

> **원자재(WTI·금) 주의** — 선물 연결 데이터라 월간 시계열에 결측 구간이 생길 수 있습니다. 현재 `y`가 9개인 것은 그 때문이며, 결측을 임의로 채우지 말고 실제 있는 값만 넣습니다.

### 2-1b. 종가 확인 요령 (반복해서 겪은 함정들)

- **툴이 "연결 안 됨"으로 나와도 바로 포기하지 않습니다.** MCP 데이터 도구(koreaStock, UsStockInfo, Alpha Vantage 등)는 세션 중간에 끊겼다가 다시 붙는 경우가 흔합니다. 한 번 실패하면 다른 지표부터 먼저 처리하고, 잠시 후(또는 마지막에) 다시 시도해보세요. 못 붙으면 그때 웹 검색으로 넘어갑니다.
- **웹 검색은 반드시 영어 + 명시적 날짜로.** "8월 4일 뉴욕증시 마감"처럼 한글+상대적 날짜로 검색하면, 미국 장이 한국시간 새벽에 끝나는 특성상 "8월 4일 아침 보도"가 실제로는 "8월 3일 장" 얘기인 경우가 많아 **이미 아는 전날 수치가 오늘 수치인 것처럼** 튀어나옵니다. `"August 4, 2026" S&P 500 close` 식으로 영어+정확한 날짜를 박아서 검색하면 이 문제가 크게 줄어듭니다. 검색 결과가 이미 저장된 전일 값과 완전히 같으면(우연이 아니라면) 라벨이 잘못됐다고 의심하세요.
- **니케이225(^N225)**: 실시간 조회 도구의 날짜 라벨이 실제 도쿄 거래일보다 **하루 앞서(-1일)** 찍힙니다. 예: 도구가 "07-30"이라고 표시한 종가가 실제로는 일본시간 7/31 종가. 아는 값 하나와 대조해서 오프셋을 먼저 확인한 뒤 사용하세요.
- **금(Gold)**: `GC=F` 같은 COMEX 선물 티커는 날짜 라벨이 들쭉날쭉해서(결제 시각 vs 데이터 벤더 표기 차이) 신뢰하기 어렵습니다. 대신 **XAUUSD 현물 시세의 일별 시계열**(예: Alpha Vantage `GOLD_SILVER_HISTORY`, interval=daily)을 쓰면 날짜가 깔끔하게 맞습니다.
- **WTI**: 원유 결제(official settlement) 수치는 유료/막힌 사이트(NYMEX, Barchart 등)에 있는 경우가 많습니다. 뉴스가 "장중가"라고 스스로 명시한 숫자는 종가로 쓰지 마세요.
- 그래도 확인이 안 되면 — 원래 원칙대로 **전일 값 유지 + 커밋 메시지에 명시**. 억지로 끼워맞추지 않습니다.

### 2-2. `index.html`에서 손대는 곳

숫자 카드 외에 그날 사건이 있으면 아래를 고칩니다. **HTML 구조·CSS·클래스는 절대 바꾸지 않고, 텍스트 노드만 교체**합니다.

| 위치 | 무엇을 | 언제 |
| --- | --- | --- |
| 상단 배너 `.banner.top .rd` | `Last Updated` 날짜 | 매번 |
| `.dday .ddrow` | D-day 항목 — 지난 건 삭제, 남은 건 D-n 재계산, 새로 다가온 일정 추가. 가장 큰 이벤트 하나에만 `class="ddi hot"` | 매번 |
| 달력 `.mo` 카드 | 지난 일정의 `.ev`에 `done` 클래스 추가 + `<div class="res">✓ …</div>` 결과 상자 삽입. 결과가 좋으면 `res up`, 나쁘면 `res down` | 일정이 지날 때 |
| 달·월 전환 | 이번 달 `.mo`에 `now` 클래스와 `<span class="nowbadge">이번 달</span>`, 지난 달은 `past` + `pastbadge`로 교체 | 월초 |
| PART 2 `.tl` | ±3% 이상 급등락·서킷브레이커·중대 뉴스가 있던 날만 `.tlrow` 한 개 추가(최신이 아래). `dn`/`up`/`nt` 중 하나, 교훈이 있으면 `.lesson` 한 줄 | 큰 사건이 있을 때만 |
| `.note` 마무리 | 다가오는 최대 관문 문장 | 관문이 지나갈 때 |

### 2-3. 하지 말 것

- `<style>` 블록 수정 금지. 색·폰트·간격은 디자인 시스템에서 옵니다.
- 이모지 사용 금지 (KR / US / GL 같은 문자 표기를 씁니다).
- **빨강은 상승, 파랑은 하락**(한국식 등락 색상 — 서구식 초록/빨강 아님). 단 D-day·달력의 "빨강 = 고위험 일정" 강조는 별개 규칙입니다. 만기·수급은 진회색, 실적은 검정, 참고 경향은 연회색.
- 추정일에는 반드시 `≈`를 붙입니다. 확정 발표되면 `≈`를 떼고 정확한 날짜로 바꿉니다.
- 출처가 불확실한 수치는 넣지 않습니다. 확인이 안 되면 그 카드는 전일 값을 유지하고 갱신하지 않았음을 커밋 메시지에 적습니다.

### 2-4. 특집 페이지 (`semiconductor.html` · `credit-balance.html`)

대시보드 하단 버튼("반도체 시장 특집 →" · "국내 신용잔고 →")으로 열리는 별도 페이지 두 개도 **매일 함께 갱신**합니다. `index.html`과 마찬가지로 `<style>` 블록·구조는 건드리지 않고, 두 페이지의 내용만 고칩니다.

| 페이지 | 무엇을 | 언제 |
| --- | --- | --- |
| 공통 | 상단·하단 배너 `.rd` (Last Updated) | 매번 |
| 공통 | `.insight p` — "오늘의 인사이트" 한 단락. 그날 상황에 맞게 다시 씁니다 | 매번 |
| `semiconductor.html` | 지역별(`KR`·`US`·`CN`·`JP`·`EU`) `.newslist`에 최근 1개월 이내 뉴스만 유지 — 새 기사를 `.nitem`으로 추가하고, 1개월 지난 항목은 삭제. 각 항목은 실제 기사 링크(`.nsrc`)를 답니다 | 매번(새 뉴스가 있을 때) |
| `credit-balance.html` | `.stats`의 6개 수치, `.rtable`의 월별 반대매매 **비중(%) 행과 금액(억원) 행 둘 다** — 최신 신용융자·반대매매 자료로 갱신 | 새 수치가 확인될 때 |

확인이 안 되는 수치·기사는 넣지 않고 이전 내용을 유지 — 다른 카드와 같은 원칙입니다. 두 페이지 모두 우측 상단에 스크롤을 따라다니는 `.backbtn`("← Dashboard로 돌아가기")이 있으니 구조는 그대로 두세요.

**`credit-balance.html`의 `.stats` 6개 카드 — 매일 반드시 갱신:**
1. 신용융자 잔고 (가장 최근 발표일 기준)
2. **전일 반대매매 금액** — 가장 최근 발표된 하루치 반대매매 금액(원 단위). 금융투자협회 자료가 보통 1~3영업일 지연 발표되므로, "전일"은 "가장 최근 발표일"을 뜻합니다. 라벨의 날짜도 그 발표일로 맞춥니다.
3. **해당월 누적 반대매매 금액** — 그 달 1일부터 지금까지 반대매매 금액을 합산. 월이 바뀌면 0부터 다시 쌓기 시작하고, 지난달 최종 누적치는 `.sc`(부제) 비교 문구("전월 대비")로 남깁니다.
4. 위탁매매 미수금 (가장 최근 발표일 기준)
5. 반대매매 비중 (해당월, %) — `.rtable`의 마지막 칼럼과 값을 맞춥니다.
6. 서킷브레이커 발동 횟수 (해당월)

전일·월 누적 반대매매 수치를 못 찾으면 이전 값을 유지하고 커밋 메시지에 갱신 못 했음을 적습니다 — 다른 수치와 동일한 원칙입니다.

---

## 3. 모바일에서 시키는 방법

### 준비 (한 번만, PC/모바일 아무 데서나)

1. Claude 앱 → 좌하단 프로필/설정 → **Connectors**(연결 도구) → **GitHub** 연결 → `skda0228/Major-market-drivers` 리포 접근 허용.
2. 같은 설정 화면에서 **웹 검색(Web search)** 이 켜져 있는지 확인. 종가·뉴스를 찾아야 하므로 필수입니다.

### 매일 (모바일 Claude 앱 새 대화)

아래를 그대로 보내면 됩니다.

> `skda0228/Major-market-drivers` 리포의 `UPDATE.md`를 먼저 읽고, 그 규칙대로 오늘자로 업데이트해줘. 웹에서 오늘 종가를 확인하고 `indicators.json`을 갱신한 뒤, 필요하면 `index.html`의 D-day와 달력 결과도 고쳐서 커밋해줘.

아티팩트(폰에서 바로 보는 화면)까지 같이 맞추려면 위 문장 뒤에 한 줄 더 붙입니다.

> 그리고 이 아티팩트도 같은 내용으로 갱신해줘: https://claude.ai/code/artifact/add49568-715e-46df-9b77-75da6a1279e8

(URL을 빼면 새 아티팩트가 하나 더 생겨 링크가 갈라집니다. 반드시 붙이세요.)

- Claude가 검색 → 파일 수정안 제시 → **"커밋해줘"** 라고 확인하면 리포에 반영됩니다.
- 커밋되면 GitHub Pages가 1~2분 안에 자동 재배포됩니다. 페이지 새로고침으로 확인.
- 숫자만 빠르게 고치고 싶으면: **"indicators.json만 오늘 종가로 갱신해줘"**
- 사건이 있던 날: **"오늘 코스피 -4% 급락 원인 찾아서 PART 2 히스토리에 한 줄 추가하고, 배운 것도 써줘"**

### 잘 되게 하는 요령

- 새 대화마다 첫 줄에 **"UPDATE.md 먼저 읽어"** 를 넣으세요. 규칙을 안 읽으면 디자인을 임의로 바꿉니다.
- 커밋 전에 **"어떤 줄이 바뀌는지 먼저 보여줘"** 라고 하면 사고를 막을 수 있습니다.
- 페이지가 깨졌으면 **"직전 커밋으로 되돌려줘"** — 리포가 곧 백업입니다.
- 판단·해석 문장("배운 것", "왜 움직였나")은 초안만 받고 직접 다듬는 걸 권합니다. 이 페이지의 가치는 숫자가 아니라 그 해석에 있습니다.

### PC로 돌아왔을 때

모바일에서 커밋하면 **PC의 `market-calendar/` 폴더가 뒤처집니다.** PC에서 다시 작업하기 전에 리포 최신본을 받아 프로젝트 폴더에 덮어쓰고 시작하세요. 안 그러면 모바일 갱신분을 되돌리는 커밋을 하게 됩니다.
