# CLAUDE.md

이 리포에서 작업하기 전에 **`UPDATE.md`를 먼저 전부 읽으세요.** 아래는 그 규칙의 핵심 요약이며, 상세·예외 사항은 항상 `UPDATE.md` 원문이 우선합니다.

## 절대 하지 말 것
- `index.html`의 `<style>` 블록, HTML 구조, 클래스명은 수정 금지. 오직 **텍스트 노드만** 교체합니다.
- 이모지 사용 금지 (KR / US / GL 같은 문자 표기를 씁니다).
- 출처가 불확실한 수치는 넣지 않습니다. 확인이 안 되면 해당 카드는 **전일 값을 유지**하고, 갱신하지 않았음을 **커밋 메시지에 명시**합니다.

## PART 2 히스토리(`.tl`)
±3% 이상 급등락·서킷브레이커·중대 뉴스가 있던 날은 `.tlrow`를 추가합니다. 그날의 종가와 뉴스를 근거로 **원인 설명과 "배운 것" 문장을 Claude가 직접 작성**합니다. 추측이 아니라 확인된 수치·뉴스에 근거해 쓰고, 근거가 불확실하면 그렇게 명시합니다.

## 매일 갱신 체크리스트 (`indicators.json`)
- `asOf` / `value` / `change`를 오늘자로 갱신.
- `q` (일간 종가, 21~23개): 맨 앞을 하나 빼고 오늘 종가를 맨 뒤에 추가해 길이 21~23 유지. 휴장일에는 아무것도 추가하지 않습니다.
- `y` (월간 종가, 12개): 달이 바뀔 때만 맨 앞을 빼고 지난달 종가를 맨 뒤에 추가. 월중에는 **맨 뒤 값만 교체**.
- `y`·`q` 각각의 **마지막 값과 `value`는 반드시 일치**시킵니다.
- 필요하면 `index.html`의 `.banner.top .rd` (Last Updated — `YYYY.MM.DD HH:MM` 형식, 실제 갱신 시각까지 표기)와 `.dday .ddrow` (D-day)를 재계산합니다. 하루 2번(KST 06:00 · 15:40) 자동 갱신되므로 시각 표기가 중요합니다.
- 상승=빨강 / 하락=파랑 (한국식 등락 색상). `nt`(금리·VIX·환율·유가·금)는 방향과 무관하게 회색.

## 종가 확인 요령
데이터 도구가 끊겼으면 다른 지표 먼저 처리하고 나중에 재시도. 웹 검색은 **영어 + 명시적 날짜**로("8월 4일" 대신 `"August 4, 2026"`) — 안 그러면 전날 수치가 오늘 수치로 둔갑합니다. 니케이225는 도구 날짜 라벨이 실제보다 하루 앞섬. 금은 `GC=F` 선물 대신 XAUUSD 현물 일별 시계열을 씁니다. **일별 시계열 도구(`get_historical_stock_prices`, `GOLD_SILVER_HISTORY`)에 오늘자 행이 없으면 "데이터 없음"으로 단정 말고 실시간 시세 도구(`get_stock_info`, `GOLD_SILVER_SPOT`)로 바꿔보세요** — 특히 니케이·환율·금은 이쪽이 훨씬 자주 바로 나옵니다. 상세는 `UPDATE.md` §2-1b 참고.

## 특집 페이지도 매일 갱신 (`semiconductor.html` · `credit-balance.html`)
대시보드의 "반도체 시장 특집 →" · "국내 신용잔고 →" 버튼으로 열리는 페이지입니다. Last Updated 날짜, "오늘의 인사이트" 문단, 뉴스 목록(1개월 이내만 유지)을 함께 갱신합니다. `credit-balance.html`은 **전일 반대매매 금액**과 **해당월 누적 반대매매 금액**을 매일 갱신합니다(월이 바뀌면 누적은 0부터 다시). 상세 규칙은 `UPDATE.md` §2-4 참고.
