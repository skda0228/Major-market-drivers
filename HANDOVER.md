# HANDOVER.md — 이 리포의 "매일 자동 갱신" 업무 인수인계

이 문서는 `skda0228/Major-market-drivers`의 **매일 시장 데이터 갱신 작업**을 다른 AI/에이전트가 이어받을 때 필요한 모든 맥락을 담은 인수인계 문서입니다. Claude(Anthropic)가 수일간 이 리포를 운영하며 겪은 시행착오·확정된 방법론·미해결 이슈를 그대로 정리했습니다.

**먼저 할 일**: `CLAUDE.md`와 `UPDATE.md`를 전부 읽으세요. 그 두 파일이 "무엇을 어떻게 고치는가"에 대한 1차 규칙서이고, 이 문서는 그 위에 **왜 그렇게 하게 됐는지, 어떤 함정을 겪었는지, 이 환경(Claude Code / Claude Code Remote)에 특화된 제약이 무엇인지**를 보충하는 운영 기록입니다. 규칙이 서로 다르게 보이면 `UPDATE.md` 원문이 최종 우선순위입니다.

---

## 1. 이 프로젝트가 뭔가

정적 GitHub Pages 사이트(빌드 과정 없음, 순수 HTML/CSS/JS + JSON 데이터 파일). 한국 투자자를 위한 "시장 주요 지표 + 급등락 히스토리 + 인자 사전" 대시보드입니다.

- **`index.html`** — 메인 대시보드. `<style>` 블록 내장, 클라이언트 사이드 JS가 `indicators.json`을 `fetch`해서 카드/그래프를 렌더링.
- **`indicators.json`** — 매일 갱신되는 데이터 소스. 주요 지표 10종, 글로벌 지수 7개(`others`), 각 지표의 1년/1개월 시계열.
- **`styles.css`** — 디자인 시스템 토큰(거의 손댈 일 없음).
- **`semiconductor.html`** — 반도체 시장 특집 페이지(대시보드 버튼으로 새 탭에서 열림). 매일 갱신 대상.
- **`credit-balance.html`** — 국내 신용잔고/반대매매 특집 페이지. 매일 갱신 대상.
- **`CLAUDE.md`** — 핵심 규칙 요약(빠르게 참고용).
- **`UPDATE.md`** — 규칙 원문. 상세 스키마, 하지 말 것, 데이터 소싱 요령, 특집 페이지 체크리스트가 전부 여기 있음.
- **`HANDOVER.md`** — 이 문서.

배포는 GitHub Pages가 `main` 브랜치를 자동 빌드/배포합니다. **PR이 `main`에 merge되는 순간이 곧 배포 트리거**입니다.

---

## 2. 매일 해야 하는 일 (한 줄 요약)

한국·미국 증시 둘 다 열린 거래일이면, 그날 미국 장 마감(한국시간 새벽) 이후 시점에:

1. 주요 지표 10종(코스피·S&P500·나스닥·다우존스·니케이225·美10년물·VIX·원/달러·WTI·금) 종가를 **실시간 조회**해서 `indicators.json` 갱신.
2. 글로벌 지수 7개(`others`: 상하이·항셍·베트남·유로스톡스50·FTSE100·DAX·SENSEX)도 실시간 갱신.
3. ±3% 이상 급등락·서킷브레이커·중대 뉴스가 있었으면 `index.html`의 PART 2 히스토리(`.tl`)에 원인 설명 + "배운 것" 문장을 **직접 작성**해서 새 항목 추가.
4. `index.html`의 Last Updated 배너, "마지막 거래일" 문구, D-day 카운트다운을 재계산.
5. `semiconductor.html` / `credit-balance.html`의 Last Updated·인사이트·뉴스도 갱신(새로 확인된 내용이 있을 때만 — 없으면 손대지 않음).
6. git 브랜치 생성 → 커밋 → push → PR → merge까지 **끝까지 완료**.

휴장일(주말·공휴일)이면 아무 파일도 건드리지 않고 종료합니다.

세부 스키마와 정확한 규칙은 반드시 `UPDATE.md` §2 원문을 참고하세요 — 요약이 아니라 원문 기준으로 작업해야 합니다.

---

## 3. 절대 규칙 (위반하면 안 되는 것)

- **`index.html`의 `<style>` 블록, HTML 구조, 클래스명은 수정 금지.** 오직 텍스트 노드만 교체. (예외: 사용자가 명시적으로 디자인 변경을 요청한 경우만 — 지금까지 여러 차례 있었음. 자동 일일 갱신 루틴에서는 절대 구조를 바꾸지 마세요.)
- **이모지 사용 금지.** KR/US/GL 같은 문자 표기를 씁니다 (`.mk` 클래스 태그).
- **출처가 불확실한 수치는 절대 넣지 않습니다.** 확인이 안 되면 그 카드는 전일 값을 그대로 유지하고, 갱신 못 했다는 사실을 커밋 메시지에 명시합니다. 억지로 끼워맞추거나 추정치를 실측치처럼 쓰지 않습니다.
- **상승 = 빨강, 하락 = 파랑** (한국식 등락 색상 — 서구식 초록/빨강 아님). 단 금리·VIX·환율·유가·금(`dir:"nt"`)은 방향과 무관하게 회색.
- `y`/`q` 시계열 배열의 **마지막 값과 카드의 `value`는 반드시 일치**시킵니다.
- 새 기능을 추가하고 싶어도(예: 새 지표, 새 섹션) **먼저 데이터 소스가 안정적으로 확보 가능한지 확인**하세요. 확보가 안 되면 기능 자체를 넣지 않는 게 맞습니다 (§6의 "야간선물" 사례 참고).

---

## 4. 데이터 소싱 방법론 — 수일간 겪은 함정과 해법

이 섹션이 이 문서에서 가장 중요합니다. 아래 문제들을 반복해서 겪었고, 전부 실제로 데이터가 틀리게 들어갈 뻔했던 사례입니다.

### 4-1. 실시간 조회 vs 일별 시계열 조회— 반드시 실시간부터 시도
과거 종가 시계열을 반환하는 도구(예: `get_historical_stock_prices`, Alpha Vantage `GOLD_SILVER_HISTORY`)는 **벤더 쪽 반영이 늦어서 당일자 행이 몇 시간 늦게 뜨는 경우가 흔합니다** — 특히 니케이225·환율·금. 이걸 "데이터가 없다"로 단정하고 포기하면 안 됩니다. **실시간 시세 조회 도구**(예: `get_stock_info`, `GOLD_SILVER_SPOT`)로 바꿔보면 훨씬 자주 바로 나옵니다.

`get_stock_info` 계열 도구가 반환하는 `marketState` 필드를 꼭 확인하세요:
- `PREPRE` (오늘 장 시작 전) — `previousClose`는 **그저께 종가**, `regularMarketPrice`(+`regularMarketChangePercent`)가 **방금 끝난 어제 세션의 종가·등락률**입니다. 이 필드를 헷갈리면 하루 전 수치를 오늘 수치로 잘못 넣게 됩니다.
- `REGULAR` / `POST` / `POSTPOST` — 시장이 열려 있거나 막 닫힌 상태. `regularMarketPrice`가 최신값.
- 원자재(WTI)·FX(USD/KRW)·금(XAU/USD)는 **거의 24시간 연속 거래**되기 때문에 "오늘의 공식 종가" 개념이 모호합니다. 이때는:
  - **자체 기록(직전 저장값)과 비교해서 일관되게 델타를 계산**하세요. API가 자체적으로 주는 `previousClose`가 우리가 전날 저장한 값과 다를 수 있는데(벤더 데이터 리비전), 그럴 땐 API의 `regularMarketPrice`(최신값)를 신뢰하고 **변화율은 우리 쪽 저장값 기준으로 재계산**하는 편이 시계열 일관성이 유지됩니다.
  - WTI처럼 결제(official settlement) 개념이 있는 자산은, 실시간 티커 값과 뉴스가 보도하는 "정산가"가 다를 수 있습니다 — 뉴스가 "장중가"라고 스스로 명시한 숫자는 종가로 쓰지 마세요. 애매하면 영어+명시적 날짜 뉴스 검색으로 교차 검증하세요.

### 4-2. 웹 검색은 반드시 "영어 + 명시적 날짜"
"8월 4일 뉴욕증시 마감"처럼 한글+상대적 날짜로 검색하면, 미국 장이 한국시간 새벽에 끝나는 특성상 "8월 4일 아침 보도"가 실제로는 "8월 3일 장" 얘기인 경우가 많습니다 — **이미 아는 전날 수치가 오늘 수치인 것처럼** 튀어나옵니다. `"August 4, 2026" S&P 500 close` 식으로 **영어 + 정확한 날짜**를 박아서 검색하면 이 문제가 크게 줄어듭니다. 검색 결과가 이미 저장된 전일 값과 완전히 같으면(우연이 아니라면) 라벨이 잘못됐다고 의심하세요.

### 4-3. 니케이225 — 날짜 라벨이 하루 앞서 찍힘
`^N225` 실시간 조회 도구의 날짜 라벨이 실제 도쿄 거래일보다 **하루 앞서(-1일)** 찍히는 경우가 있었습니다(도구/벤더에 따라 다를 수 있으니, 아는 값 하나와 대조해서 오프셋을 먼저 확인).

### 4-4. 금(Gold) — 선물 대신 현물 시계열
`GC=F` 같은 COMEX 선물 티커는 날짜 라벨이 들쭉날쭉해서 신뢰하기 어렵습니다. 대신 **XAUUSD 현물 시세**(Alpha Vantage `GOLD_SILVER_SPOT`/`GOLD_SILVER_HISTORY` 등)를 쓰면 훨씬 안정적입니다.

### 4-5. 도구가 "연결 안 됨"으로 나와도 바로 포기하지 않기
MCP 데이터 도구는 세션 중간에 끊겼다가 다시 붙는 경우가 흔합니다(실제로 이 세션에서도 대화 도중 여러 도구가 반복적으로 disconnect → reconnect 됐습니다). 한 번 실패하면 다른 지표부터 먼저 처리하고, 잠시 후 다시 시도하세요. 진짜 못 붙으면 그때 웹 검색으로 넘어갑니다.

### 4-6. 값 하나를 여러 소스로 교차 검증하는 습관
특히 ±3% 이상 급변동이 있었던 날은 반드시 뉴스 검색으로 한 번 더 교차 확인하세요(예: KOSPI -4.58% 서킷브레이커 사건 때 실시간 티커 값과 별개로 뉴스 3~4곳에서 "6,296.38, -4.58%, 서킷브레이커 발동"이 정확히 일치하는 걸 확인 후 반영). 실시간 티커 하나만 믿고 큰 사건을 기록하지 마세요.

---

## 5. Git / GitHub 워크플로우

이 리포는 **직접 `main`에 커밋하지 않습니다.** 매번:

```
git checkout main && git pull origin main
git checkout -b <설명적인-브랜치명>
# indicators.json, index.html 등 수정
python3 -c "import json; json.load(open('indicators.json'))"   # JSON 유효성 검증
grep -o '<div' index.html | wc -l   # </div> 개수와 비교해서 태그 균형 확인
grep -o '</div>' index.html | wc -l
git add <바뀐 파일들>
git commit -m "<왜 바뀌었는지 설명하는 커밋 메시지>"
git push -u origin <브랜치명>
```

그 다음 GitHub PR을 만들고 **바로 merge까지 완료**합니다(사람 승인을 기다리지 않음 — 이 리포는 자동 일일 갱신이 전제된 운영 방식). PR 본문에는 무엇을 갱신했는지, 어떤 지표가 확인 안 됐는지(있다면)를 적습니다.

**커밋 전 체크리스트**:
- [ ] `indicators.json`이 유효한 JSON인가
- [ ] 모든 지표의 `q`/`y` 배열 마지막 값이 `value`와 일치하는가
- [ ] `<div>`/`</div>` 개수가 같은가(구조를 안 건드렸다는 증거)
- [ ] (가능하면) 헤드리스 브라우저로 로컬 렌더링해서 카드가 깨지지 않는지 눈으로 확인 — `python3 -m http.server`로 로컬 서빙 후 Playwright/Chromium으로 스크린샷

---

## 6. 시도했다가 접은 것 — "코스피200 야간선물" 섹션

2026-08-06~07 사이에 주요 10종 카드 아래에 **코스피200 야간선물(CME/Eurex 연계, 18:00~05:00 KST 거래)** 을 보여주는 섹션을 실제로 구현했었습니다(`nightFutures` JSON 필드 + `.night` CSS/HTML/JS). 하지만:

- 이 값을 확인할 수 있는 실시간 API/티커를 못 찾았고, 유일한 소스는 국내 증권사 리서치("오늘의 투자전략")를 인용한 뉴스 기사였는데, **이 기사가 보통 07:50~09:00 KST경에야 나와서** 06:30 자동 갱신 시점에는 거의 항상 데이터가 없었습니다.
- `m.stock.naver.com`, `sonmul.co.kr` 같은 페이지에 이 데이터가 있는 걸 사용자가 확인해줬지만, **이 세션(Claude Code 환경)의 아웃바운드 네트워크 정책이 두 도메인 모두 차단**하고 있어서(`403 policy denial`, 프록시 게이트웨이 단에서 거부) 직접 접근이 불가능했습니다.
- 결국 사용자 판단으로 **기능 자체를 되돌렸습니다** (PR #20에서 추가 → PR #21에서 전량 제거).

**만약 다른 AI/환경에서 이 작업을 받는다면**: 그 환경은 네트워크 정책이 다를 수 있으니, `m.stock.naver.com/domestic/index/FUT/total` 같은 페이지에 직접 접근 가능하다면 이 기능을 다시 시도해볼 가치가 있습니다. 단, 되살릴 때도 **확인 안 되는 날엔 절대 숫자를 지어내지 말고 "확인 전" 상태를 유지**하는 원칙은 그대로 지켜야 합니다. (참고용으로 당시 구현했던 JSON 스키마: `nightFutures: {name, value, change, dir, note}` — git log에서 PR #20/#21 diff를 보면 정확한 구현이 남아있습니다.)

---

## 7. 이 환경(Claude Code / Claude Code Remote) 특유의 인프라 제약

**주의**: 이 섹션은 Claude Code / Claude Code Remote(CCR) 플랫폼에 특화된 내용입니다. 다른 AI 플랫폼으로 옮긴다면 아래 도구 이름·제약은 그대로 적용되지 않을 수 있지만, **"자동화 세션은 매번 새로 시작되고 매번 리포/자격증명을 다시 확보해야 한다"는 일반 원칙**은 어떤 플랫폼이든 비슷하게 적용될 가능성이 높으니 참고하세요.

- **자동 실행 세션은 매번 새로 스폰되고, 리포에 대한 push 권한이 자동으로 있지 않습니다.** 매번 리포를 세션에 명시적으로 "등록"하는 스텝이 0단계로 필요했습니다(CCR에서는 `add_repo` 도구 + `register_repo_root`).
- **개인 커넥터(PlayMCP, Alpha Vantage 등)는 자동화 세션에 붙지 않습니다.** `create_trigger`의 `connectors` 파라미터를 이 조직(org)에서 명시적으로 차단해놨습니다("the connectors parameter is not available for this organization") — 여러 차례 재확인했고 여전히 막혀 있습니다. 즉 **자동 실행 세션은 실시간 시세 MCP 도구 없이 웹 검색만으로 버텨야 하는 구조**입니다. 이게 자동화 신뢰성이 떨어지는 근본 원인 중 하나입니다.
- **`github` MCP 도구는 개인 커넥터가 아니라 환경 내장 도구**라서 위 제약과 무관하게 보통 자동 세션에도 있어야 정상이지만, 실제로 한 번은 자동 세션이 "add_repo도 github 도구도 전혀 안 보였다"고 보고한 사례가 있었습니다 — MCP 서버 연결이 세션 시작 직후 몇 초~몇 분 늦게 붙는 경우가 있어 보이니, **도구가 안 보인다고 바로 포기하지 말고 몇 차례 재시도**하는 로직을 자동화 프롬프트에 반드시 넣어야 합니다.
- **git push가 403으로 거부**되는 문제를 여러 차례 겪었습니다. 원인은 매번 달랐습니다(GitHub App 권한 재연결 필요 / add_repo 미실행 / 세션이 애초에 리포 접근 권한이 없음). git push가 실패하면 **GitHub API 도구(`create_or_update_file`, `push_files` 등)로 대신 커밋하는 폴백**을 반드시 실제로 실행하도록 지시해야 합니다(실패 보고만 하고 멈추는 걸 방지).
- **스케줄 트리거(CCR Routine)를 재생성할 때 타이밍에 주의**하세요. 다음 실행 슬롯이 지나기 직전/직후에 트리거를 새로 만들면 그날 실행이 통째로 스킵될 수 있습니다(실제로 한 번 발생).
- **현재 이 리포에는 이미 CCR Routine이 등록되어 있습니다** — 매일 06:30 KST(월~금, 주말 제외) 자동 실행, trigger id `trig_01DhiZcfAVnYpatY2j7jCPg4`. **다른 AI/플랫폼으로 이 작업을 완전히 이관한다면 이 트리거를 비활성화하거나 삭제하는 걸 검토하세요** — 안 그러면 두 자동화가 같은 날 각자 PR을 올려서 충돌하거나 중복 커밋이 생길 수 있습니다.

---

## 8. 특집 페이지 체크리스트 (요약 — 원문은 `UPDATE.md` §2-4)

`semiconductor.html`·`credit-balance.html`은 대시보드와 별개 페이지지만 **매일 함께 갱신 대상**입니다. 단, 이 세션에서는 시간/우선순위 제약으로 "새로 확인된 뉴스가 없으면 억지로 날짜만 바꾸지 않는다"는 원칙을 지켰습니다 — 인수인계 받는 쪽도 이 원칙을 지키세요. **날짜만 바꾸고 내용은 그대로 두면 "오늘 새로 확인했다"는 거짓 신호를 주게 됩니다.**

- 공통: 상단·하단 `.rd`(Last Updated), `.insight p`(오늘의 인사이트 한 단락) — 그날 상황에 맞게 다시 씀.
- `semiconductor.html`: 지역별(KR/US/CN/JP/EU) `.newslist`에 최근 1개월 이내 뉴스만 유지.
- `credit-balance.html`: `.stats` 6개 카드(신용융자 잔고, **전일 반대매매 금액**, **해당월 누적 반대매매 금액**, 위탁매매 미수금, 반대매매 비중, 서킷브레이커 횟수) + `.rtable`의 월별 비중(%)·금액(억원) 행 둘 다 갱신.

---

## 9. 지금까지의 사고 이력 (짧은 요약)

향후 비슷한 문제가 또 나올 수 있으니 참고용으로 남깁니다.

1. **git push 403** — 잘못된 GitHub App("Claude Design Import", read-only)이 연결돼 있었음. 올바른 "Claude" 앱으로 재연결 후 해결.
2. **PART2 히스토리에 "배운 것" 문장을 안 넣던 시기** — 규칙을 잘못 해석해서 생략했었는데, 사용자가 "그날그날 종가를 보고 적절히 의견을 넣어달라"고 명시적으로 정정. 지금은 **Claude가 원인+교훈을 직접 작성하는 게 맞는 규칙**입니다.
3. **니케이/환율/금이 반복적으로 "확인 안 됨"으로 남던 문제** — §4-1의 실시간 vs 시계열 도구 구분을 몰라서 발생. 해법 확립 후 재발 없음.
4. **자동화 첫 실행 실패** — 리포가 세션에 등록 안 됨 → `add_repo` 0단계 스텝 추가로 해결.
5. **트리거 재생성 타이밍으로 하루치 실행 스킵** — §7 참고.
6. **자동 세션이 add_repo/github 도구를 아예 못 찾음** — MCP 서버 연결 지연 추정, 재시도 로직 강화로 대응 중(재발 여부 계속 관찰 필요).
7. **야간선물 기능 추가 → 네트워크 정책으로 데이터 소스 접근 불가 확인 → 기능 철회**. §6 참고.

---

## 10. 요약: 새로 인수받는 AI가 첫 실행에서 할 일

1. `CLAUDE.md`, `UPDATE.md`, 이 문서(`HANDOVER.md`)를 전부 읽는다.
2. 리포에 대한 쓰기 권한을 확보한다(플랫폼에 맞는 방법으로 — 필요하면 사람에게 물어본다).
3. `indicators.json`의 현재 `asOf` 날짜를 확인해서, 마지막으로 언제까지 갱신됐는지 파악한다.
4. 오늘이 거래일인지 확인한다(한국·미국 증시 둘 다).
5. §4의 방법론대로 10개 지표 + 글로벌 지수 7개를 실시간 확인한다.
6. §5의 git 워크플로우로 커밋·PR·merge까지 완료한다.
7. 확인 못 한 지표가 있으면 정직하게 전일 값 유지 + 커밋 메시지에 명시한다. 절대 지어내지 않는다.
